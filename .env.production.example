# Variáveis para build de produção (Railway / deploy)
# Defina no dashboard do Railway (Variables) ou use em .env.production local

# URL da API do backend (obrigatório em produção)
VITE_API_BASE_URL=https://seu-backend.up.railway.app

# Opcional: timeout da API em ms
# VITE_API_TIMEOUT=30000
