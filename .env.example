# ===========================================
# Desenvolvimento local (vite.config.ts)
# ===========================================
# Host do servidor (dev e preview)
HOST=0.0.0.0
# Porta do servidor (dev e preview)
PORT=5173
# Hosts permitidos (server.allowedHosts e preview.allowedHosts). Lista separada por vírgula.
# Pode ser hostname ou URL completa (ex.: https://portal-colaboradores.up.railway.app)
ALLOWED_HOSTS=localhost,127.0.0.1

# ===========================================
# API (embutido no build se usar VITE_*)
# ===========================================
# URL da API do backend
VITE_API_BASE_URL=http://localhost:8000
# Timeout da API em ms (opcional)
# VITE_API_TIMEOUT=30000
