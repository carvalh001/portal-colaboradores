# Variáveis para build e preview em produção (Railway / deploy)
# Defina no dashboard do Railway (Variables) ou use em .env.production local

# ---------- Vite (server / preview) ----------
# Host do servidor
HOST=0.0.0.0
# Porta do servidor
PORT=8080
# Hosts permitidos (evita "Blocked request. This host is not allowed").
# Inclua o domínio público do app (ex.: portal-colaboradores.up.railway.app).
ALLOWED_HOSTS=portal-colaboradores.up.railway.app,localhost,127.0.0.1

# ---------- API (embutido no build) ----------
# URL da API do backend (obrigatório em produção)
VITE_API_BASE_URL=https://seu-backend.up.railway.app
# Timeout da API em ms (opcional)
# VITE_API_TIMEOUT=30000
